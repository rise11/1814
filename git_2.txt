git confing --golbal user.name ""
git confing --golbal user.email ""

git init 初始化本地仓库

git add 文件名 ：加入暂存区
git add -A

git commit -m "注释" 提交

git status 查看当前状态

git diff 查看哪里更改了

git reset --hard 版本号 =》版本还原

git reflog/log 查看版本信息

git remote add origin 项目路径

git remote 远程仓库设置; 设置成  add  名字叫做 origin（代表项目路径） 项目路径

git push -u origin master 本地仓库推送至远端仓库